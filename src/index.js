import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
// .render(what you want to render, where you want to render it)
ReactDOM.render(<App />, document.getElementById('root'));
